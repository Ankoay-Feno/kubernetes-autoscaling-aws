terraform {
  required_version = ">= 1.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
    kubernetes = {
      source  = "hashicorp/kubernetes"
      version = "~> 2.23"
    }
    tls = {
      source  = "hashicorp/tls"
      version = "~> 4.0"
    }
  }
}

provider "aws" {
  region = var.aws_region
}

# # Provider Kubernetes basique - sera reconfiguré après la création du cluster
# provider "kubernetes" {
#   config_path = "~/.kube/config"  # Utilise le kubeconfig local
# }